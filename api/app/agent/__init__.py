"""
Agentic auto-apply — DLD §4.1, principle P-3 (recommend-then-act with HITL).

The agent reads the recommendation queue, evaluates each NEW rec against a
configurable policy, and auto-applies those that pass.  Default: OFF.
"""
