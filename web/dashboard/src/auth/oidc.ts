/**
 * OIDC helpers — no longer used.
 *
 * Authentication now uses direct Maximo username/password via POST /auth/login.
 * This file is kept to satisfy any stale imports during the transition.
 *
 * @deprecated Safe to delete once all imports are removed.
 */
export {};
