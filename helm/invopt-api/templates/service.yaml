apiVersion: v1
kind: Service
metadata:
  name: {{ include "invopt-api.fullname" . }}
  labels:
    {{- include "invopt-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "invopt-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
